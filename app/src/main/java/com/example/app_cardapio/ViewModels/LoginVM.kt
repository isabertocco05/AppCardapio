package com.example.app_cardapio.ViewModels

import android.content.Context
//import com.google.firebase.auth.FirebaseAuth

class LoginVM (private val context: Context) {
//    private val autentica: FirebaseAuth =  FirebaseAuth.getInstance()

//    // Autenticar com Firebase

//    fun authenticateUser(user: User, onResult: (String?) -> Unit) {
//        if (user.email.isEmpty() || user.password.isEmpty()) {
//            onResult("Todos os campos devem ser preenchidos.")
//            return
//        }
//
//        // Autenticar com Firebase
//        autentica.signInWithEmailAndPassword(user.email, user.password)
//            .addOnCompleteListener { task ->
//                if (task.isSuccessful) {
//                    onResult(null) // Autenticação bem-sucedida

                        // clicar no botão e intent para tela de cardápio

//                } else {
//                    onResult("Email ou senha incorretos.")
//                }
//            }
//    }
}